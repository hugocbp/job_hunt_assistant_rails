language: ruby
rvm:
  - 2.6.1
before_script:
  - bundle exec rails db:create --all
  - bundle exec rails db:migrate
script:
  - bundle exec rails rspec
services:
  - postgresql
addons:
  postgresql: "9.4"
